import React from "react";
import styled from "styled-components";

const AccountSideMenu = () => {
  return <AccountMenuContainer></AccountMenuContainer>;
};

const AccountMenuContainer = styled.div``;
export default AccountSideMenu;
