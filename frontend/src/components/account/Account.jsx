import React from "react";
import styled from "styled-components";

const Account = () => {
  return <AccountMainContainer></AccountMainContainer>;
};

const AccountMainContainer = styled.div`
  display: flex;
  flex-direction: row;
`;
export default Account;
